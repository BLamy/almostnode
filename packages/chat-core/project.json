{
  "name": "chat-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/chat-core/src",
  "projectType": "library",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --project tsconfig.json --noEmit", "cwd": "packages/chat-core" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --project tsconfig.json --noEmit", "cwd": "packages/chat-core" } }
  }
}
